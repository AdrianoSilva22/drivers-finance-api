export interface Driver {
    cpf: string
    name: string
    email: string
    senha: string
    genero: string
    phone_number: string
  }

  export const trueActive = (active: boolean) => {
    return active = true
  }